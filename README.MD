Just another hyip parser...
----------------------------

Just to instal run in terminal

```sh
git clone [git-repo-url] aggr
cd aggr
npm install
```

To run app use this

```sh
cd aggr/src
node index.js
```